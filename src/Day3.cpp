//
// Created by Playstation 5 on 12/12/2019.
//

#include <Day3.h>

std::string Day3::run() {
    /**
     * make wire object
     *      each wire contains an amount of lines
     *          each line consists of two vectors
     * origin vector is 0,0
     * check each line in a wire against each line in another wire to see if there is an intersection, and if there is save the intersection vector to a list of vectors
     *
     * then check which vector has the shortest distance to the origin (Which can just be positiveX + positiveY, as some may be negative)
     */
    return {};
}

std::string Day3::prettyRun() {
    return std::string();
}

